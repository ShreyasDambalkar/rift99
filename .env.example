# ── PharmaGuard Backend Environment Variables ─────────────────────────────
# Copy this to .env in your vcf authenticator folder.
# The backend (vcf_authenticator.py) reads these for chat persistence.

# Your Supabase project URL (from Supabase Dashboard → Settings → API)
SUPABASE_URL=https://your-project-id.supabase.co

# Service role key — bypass RLS for backend writes (keep private!)
# Found in Supabase Dashboard → Settings → API → service_role
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5...
